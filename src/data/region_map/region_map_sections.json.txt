{{ doNotModifyHeader }}
#ifndef GUARD_DATA_REGION_MAP_REGION_MAP_ENTRIES_H
#define GUARD_DATA_REGION_MAP_REGION_MAP_ENTRIES_H

## for map_section in map_sections
{% if existsIn(map_section, "first_name") %}
static const u8 sMapName_{{ map_section.var_name }}[] = _("{{ map_section.name }}");
{% endif %}
## endfor
## for extra in extra_names
static const u8 sMapName_{{ extra.var_name }}[] = _("{{ extra.text }}");
## endfor

const struct RegionMapLocation gRegionMapEntries[] = {
## for map_section in map_sections
    [{{ map_section.id }}] = {
{% if existsIn(map_section, "x") %}
        .x = {{ map_section.x }},
{% else %}
        .x = 0,
{% endif %}
{% if existsIn(map_section, "y") %}
        .y = {{ map_section.y }},
{% else %}
        .y = 0,
{% endif %}
{% if existsIn(map_section, "width") %}
        .width = {{ map_section.width }},
{% else %}
        .width = 1,
{% endif %}
{% if existsIn(map_section, "height") %}
        .height = {{ map_section.height }},
{% else %}
        .height = 1,
{% endif %}
{% if existsIn(map_section, "var_name") %}
        .name = sMapName_{{ map_section.var_name }},
{% else %}
        .name = (const u8[])_(""),
{% endif %}
    },
## endfor
};

const struct RegionMapLocation gKantoRegionMapEntries[] = {
## for entry in kanto_entries
    [{{ entry.id }}] = { .x = {{ entry.x }}, .y = {{ entry.y }}, .width = {{ entry.width }}, .height = {{ entry.height }}, .name = sMapName_{{ entry.var_name }} },
## endfor
};

const struct RegionMapLocation gSeviiRegionMapEntries[] = {
## for entry in sevii_entries
    [{{ entry.id }}] = { .x = {{ entry.x }}, .y = {{ entry.y }}, .width = {{ entry.width }}, .height = {{ entry.height }}, .name = sMapName_{{ entry.var_name }} },
## endfor
};

#endif // GUARD_DATA_REGION_MAP_REGION_MAP_ENTRIES_H
